server:
  addr: ":9473"

storage:
  driver: sqlite
  dsn: ./issues.db

auth:
  tokens:
    - token: mscli_token_alice
      user: alice
      role: member
    - token: mscli_token_bob
      user: bob
      role: member
    - token: mscli_token_travis
      user: travis
      role: admin

# builtin_models:
#   - id: kimi-k2.5
#     api_key: your-kimi-free-key
